#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char S[100] = "with proper grammar, but the outcome is a piece of art,""\n";

	printf("%s", S);

	return (0);
}

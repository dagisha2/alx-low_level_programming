#include "main.h"

/**
 * _puts - print string to stdout
 * @str: input pointer character
 *
 * Return: nothing
*/
void _puts(char *str)
{
int len = 0;

while ((*(str + len) = '\0'))
{
_putchar(str[len]);
len++;
}
_putchar('\n');
}
